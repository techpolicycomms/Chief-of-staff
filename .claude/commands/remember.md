---
description: Capture a fact to today's daily note immediately
argument-hint: <fact to remember>
---

Capture this to today's daily note: **$ARGUMENTS**

1. Decide which section it belongs in: Meetings, Tasks, Decisions,
   Threads/context, or Frictions.
2. If the fact involves a person, also tag which bucket from
   `config/context-itu.md` they sit in (ITU internal / Member State /
   UN system / Sector Member / academia & civil society / host) so
   future briefs route correctly.
3. Append a single bullet to the right section in
   `memory/YYYY-MM-DD.md`. Create the file with the daily note skeleton
   if it doesn't exist.
4. If this looks like it should also live longer than a month
   (relationship state, decision of record, lesson learned, treaty or
   Council position, election commitment), propose the corresponding
   update to `MEMORY.md` or `people/<slug>.md` and wait for me to
   confirm. Never overwrite a Member State position from memory alone.
5. Confirm with one short sentence. No essay.
