---
description: Capture a fact to today's daily note immediately
argument-hint: <fact to remember>
---

Capture this to today's daily note: **$ARGUMENTS**

1. Decide which section it belongs in: Meetings, Tasks, Decisions,
   Threads/context, or Frictions.
2. Append a single bullet to the right section in
   `memory/YYYY-MM-DD.md`. Create the file with the daily note skeleton
   if it doesn't exist.
3. If this looks like it should also live longer than a month
   (relationship state, decision of record, lesson learned), propose
   the corresponding update to `MEMORY.md` or `people/<slug>.md` and
   wait for me to confirm.
4. Confirm with one short sentence. No essay.
